# travelog — 삼토의 Travelog

> 여행 준비(안내서) → 여행 중(기록) → 귀국 후(공개 여행기록)가 **한 주소에서 이어지는** 시스템.
> 2026-08-12 설계 확정 (원전: `claude-code-archive/second-brain/Inbox/사고/2026-08-12_여행가이드-시스템-해설집.md`)

---

## 이 레포가 하는 일

1. 여행 1회분의 **일정·끼니표·해설집·실무정보**를 `trips/<YYYY-MM_도시>/`에 모은다
2. 그걸 **웹페이지 하나**로 발행한다 (비공개 시작 → 원할 때 공개 전환, 도메인 없음)
3. 여행 중 원장님이 **디스코드 `#travelog`(1539498042238173205)에 사진·메모를 던지면** 해당 포인트 밑에 붙인다
   - 저장: `python tools/ingest.py --slug <포인트> --at "YYYY-MM-DD HH:MM" --memo "…" --photo <경로…>`
     → `trips/2026-09_osaka-kobe/현장/기록.md` (append) · `현장/사진/MMDD-HHMM_<슬러그>_NN.jpg` (긴 변 1600px·q80)
   - 반영: `python site/build.py` → 해당 챕터의 참고사진 다음에 「현장」 블록. 자료 0건 챕터는 아무것도 안 나오고, `미분류`는 페이지 맨 끝 「미분류 기록」으로 모인다
   - 봇이 따르는 절차의 진본: `c:\dev\jarvis-agent\.claude\commands\travelog-collect.md` (슬러그 애매하면 `미분류`, 응답은 한 줄, Discord ts는 UTC라 +9h)
   - 🔴 사진 base64 내장 구조라 **용량이 유일한 하드 제약**(아티팩트 상한 16MB). `build.py` 가 12MB 초과 시 경고한다 — 초과하면 `ingest.py` 의 `MAX_EDGE`·`JPEG_Q` 를 낮춰 재생성
4. 절차가 굳으면 `/travel-plan` 스킬로 추출한다 (**스킬은 아직 존재하지 않음** — 오사카 1회 수동 완주가 먼저)

## 산출물 3단계 (같은 주소가 시간축으로 세 번 산다)

| 시점 | 성격 | 내용 |
|---|---|---|
| 출발 전 | 안내서 | 일정표 + 끼니표 + 해설집 + 지도 + 실무정보 |
| 여행 중 | 기록으로 전환 | 현장 사진·음식 사진·메모·평가가 포인트별로 쌓임 |
| 귀국 후 | 공개 여행기록 | 공개 전환, 남에게 보여줄 형태로 정리 |

---

## 🚨 규칙

1. **「끼니표」가 전 리서치 보고의 최소 기준** (2026-08-11 원장님 승인 형식) — 이름·지표만 나열한 목록은 반려 대상. 구성: 「이게 무슨 음식인가」 설명 → 왜 여기인가 → 값·주문할 것·점수·정기휴일·1인 대응·예약·결제수단·이동시간 → 후기 칭찬/불만 양쪽 → **슬롯마다 1곳 확정 배치** → 진짜 갈리는 지점만 후보+추천 → 탈락 사유 → 예산 합계 → 확인 못 한 것.
2. **여행 일정 설계의 1단계는 「정기휴일·축일 규정 수집」** — 가고 싶은 곳 나열이 아니다. 닫힌 날을 지우면 자리가 남는다 (오사카 실전에서 배치 5건이 이 순서로 결정됨).
3. **격자형·조건부 데이터(정기휴일표·시각표·요금표)는 원문을 그대로 인용한 뒤 해석을 분리**하고, 조건과 우리 날짜를 명시 대조한다. 잘못 읽은 표는 조용히 틀린다 (사고 2건 실측).
4. **예약형 가게는 원장님 화면과 조사를 대조하는 단계를 반드시 거친다** — 로그인 뒤 화면에만 있는 플랜은 웹 조사로 안 잡힌다 (스시 아마토 얼리버드 6,000엔, 이시다야 고베규 표기 — 같은 경계면에서 2회 발생).
5. **웹 스니펫 가격·조건은 공식 페이지 원문 대조 없이 인용 금지** (가격 오인 사고 3건 실측).
6. **결정이 뒤집히면 그 결정에 딸린 리마인더·예약도 함께 재검토**한다 (운휴된 버스 예약 리마인더가 조용히 살아남은 사례).
7. 참고사진·유튜브는 **출처 표기 후 공개판에도 포함**한다 (비상업 개인 기록 — 2026-08-12 원장님 판단).
8. 코드·설정 수정 전 **사전 승인 필수** (jarvis-agent HARD 2번 동일 적용).

## 데이터 소스 1순위

| 찾는 것 | 위치 |
|---|---|
| 오사카·고베 결정 과정 전문 | `claude-code-archive/second-brain/Inbox/사고/2026-08-09~12_*.md` |
| 결정 요약·소환용 | `claude-code-archive/Wiki/Mine/여행계획-플래너-시스템-오사카실전.md` |
| 최신 확정 상태 | `claude-code-archive/second-brain/00_최근상황.md` |
| 리마인더 | `jarvis-agent/data/reminders.md` |

## 작업 기록 의무

구축·수정·중요 발견·중요 결정 완료 **즉시**:
```
python c:/dev/jarvis-agent/scripts/log_shared_context.py "한 줄 요약" --source vscode
```
